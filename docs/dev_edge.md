# Dev via Edge Proxy (Org Standard)

This repo currently exposes no HTTP dev service and does not require an edge route.

Rule:
If an HTTP web/API service is added for dev, attach it to external network `edge` and avoid host `ports:`.
