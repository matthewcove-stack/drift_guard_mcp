# AGENTS

## Doc Map
- Docs live under `docs/` (except root `README.md` and `AGENTS.md`).
- Phase docs live in `docs/phases`.
- Prompt docs live in `docs/prompts`.
- Runbooks live in `docs/runbooks`.
- Do not create new doc folders without updating `docs/repo_contract.md`.
